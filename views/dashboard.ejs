<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <%- include('layouts/publicFiles') %>
</head>
<body>
  <%- include('layouts/sidebar') %> 
  <section class="home-section">
        <section class="dash-main section">
          <div class="main-content overflow-y--scroll">
            <div class="content-grid">
              <div class="container full-width bg1">
                <div class="container-grid">
                  <div class="container-content">
                    <h1 class="hd-1 main-title">Welcome back, Liviu!</h1>
                    <p class="sub-heading col-2-3">You can start by creating the best messages for your hotel, restaurant or anything like this. Keep up with latest trends</p>
                    <a href="/<%= user.username %>/dashboard/forms">Forms</a>
                    <br>
                    <a href="/<%= user.username %>/settings/plans">See plans</a>
                    <br>
                    <p id="q1"><%= freeTrialEndDate %></p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section>
  </section>
</body>
</html>